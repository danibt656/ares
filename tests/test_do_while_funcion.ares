// Test que mezcla un do-while con una llamada a funcion
begin
{
    i32 x, y;

    proc i32 bucle() {
        do {
            output > x;
            x++;
            y = 0;
        }
        while ((x <= 6));
        return x;
    }

    x = 4;
    y = bucle();
    output > y;
}
