@ Test de un do while anidado
@ Al ejemplo del enunciado se ha añadido un tercer bucle anidado,
@ solo para mostrar que funciona
begin
{
    i32 x, y;
    x = 4;
    do {
        output(x);
        do {
            y = 0;
            output(true);
            x++;
            do {
                output(y);
                y = y + 1;
            }
            while ((y < 3));
        }
        while ((x <= 5));
        
        x++;
    }
    while ((x <= 6));
}