@ Test para los auto-operadores
begin {
    i32 x;
    x = 3;
    output > x;   // 3
    x += 3;
    output > x;   // 6
    x -= 1;
    output > x;   // 5
    x *= 2;
    output > x;   // 10
    x /= 2;
    output > x;   // 5
    x++;
    output > x;   // 6
    x--;
    output > x;   // 5
}
