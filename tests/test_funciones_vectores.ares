begin
{
    bool resultado;
	array bool[3] vector; 

	fn bool or (bool b1; bool b2; bool b3)
	{
	    bool a;
        a  = b1 || b2 || b3;
		return a;
	}

	vector[0] =  false;
	vector[1] = true;
	vector[2] = false;
	
	resultado = or (vector[0], vector [1], vector [2]);
	output(resultado);
	
}